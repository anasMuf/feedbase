{
	"name": "feedbase",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"api:dev": "pnpm run --filter=api dev",
		"admin:dev": "pnpm run --filter=admin dev",
		"platform:dev": "pnpm run --filter=platform dev",
		"dev": "pnpm --parallel --filter=api --filter=platform --filter=admin run dev",
		"lint": "pnpm exec biome check",
		"lint:fix": "pnpm exec biome check --write --unsafe",
		"prepare": "husky",
		"api:build": "pnpm run --filter=api build",
		"admin:build": "pnpm run --filter=admin build",
		"platform:build": "pnpm run --filter=platform build",
		"build": "pnpm --parallel --filter=api --filter=platform --filter=admin run build"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"packageManager": "pnpm@10.19.0",
	"devDependencies": {
		"@biomejs/biome": "2.4.2",
		"husky": "^9.1.7"
	},
	"dependencies": {
		"dotenv-cli": "^11.0.0"
	}
}
